---
layout: _innerPage.njk
title: Writing
pageDescription: Writings and musings by Bryn Newell
---

<nav class="container nav">
  <a href="/" class="breadcrumb link rosegold">return home</a>
  <a href="/" class="nav__home link rosegold">
    bryn newell
    <span class="nav__home-detail breadcrumb link rosegold">return home</span>
  </a>
</nav>

<section class="writingPage">
  <h1 class="writingPage__title">writing</h1>
  <section class="cardSection">
    {% for article in collections.article | reverse %}
    <article class="card card__clickable">
      <h3>{{ article.data.title }}</h3>
      <div class="row">
        <a href="{{ article.url }}" class="card__link link rosegold">read more</a>
        <div>
          <p class="card__category">
            {% for category in article.data.categories %}
            {{ category }} {% if not loop.last %} <span class="leMurmure">•</span> {% endif %}
            {% endfor %}
          </p>
          <p class="card__date">{{ article.data.date | formatDate }}</p>
        </div>
      </div>
    </article>
    {% endfor %}
  </section>
</section>